# AgenticOps

A small Streamlit chatbot powered by LangGraph and Groq.

## Run

```bash
uv sync
export GROQ_API_KEY="your-api-key"
uv run streamlit run app.py
```

You can instead place `GROQ_API_KEY=your-api-key` in a local `.env` file.
